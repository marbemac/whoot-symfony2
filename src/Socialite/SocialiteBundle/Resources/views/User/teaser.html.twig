<div>
    User Teaser<br />
    Score: [<span class="s_{{ object.id }}">{{ object.score }}</span>] <a class="p" href="{{ path('fos_user_user_show', {'username': object.User.username} ) }}">{{ object.User.username }}</a>
    
    {% render "SocialiteBundle:Follow:tag" with {'toObject': object.id} %}
</div>