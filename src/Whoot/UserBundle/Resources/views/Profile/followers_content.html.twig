<ul class="feed followers">
    {% for user in followers %}
        {% if not userBlocked(app.user, user) %}
            {% include "WhootUserBundle:Profile:teaser.html.twig" with {'user': user} %}
        {% endif %}
    {% endfor %}
</ul>