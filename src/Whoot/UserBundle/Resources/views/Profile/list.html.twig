<h3>New People:</h3>
<ul>
    {% for user in users %}
        <li>
            {{ user.firstName }} {{ user.lastName }}
            {% render "WhootBundle:Follow:tag" with {'toUser': user.id} %}
        </li>
    {% endfor %}
</ul>