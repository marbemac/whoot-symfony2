[
{% for event in activity %}
    {% if event.type == 'comment' %}
        {% include "WhootBundle:Comment:teaser.json.twig" with {'comment': event.comment} %}
    {% else %}
        {% include "WhootBundle:Post:activityPost.json.twig" with {'event': event} %}
    {% endif %}
    {{ loop.last ? '' : ',' }}
{% endfor %}
]