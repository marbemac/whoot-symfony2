<header id="header">
    {% block header %}

        <a class="logo" href="{{ path('homepage') }}">
            <div class="image"></div>
            The Whoot
        </a>


        <div class="nav">
            <a class="feed" href="{{ path('homepage') }}">Feed</a>
            <a class="open-invite" href="{{ path('open_invite_feed') }}">Open Invites</a>
        </div>

        <div class="search">
            <input type="text" />
        </div>

    {% endblock header %}
</header>