{% set w = width is defined ? width : dimensions %}
{% set h = height is defined ? height : dimensions %}
<img class="profileImage" src="{{ user.profileImage is defined ? '/slir/w'~w~'-h'~h~'-c1:1'~user.profileImage : gravatar(app.user.email, dimensions, 'r', 'wavatar') }}" />